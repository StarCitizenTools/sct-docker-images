# Error Logging
error_reporting = E_ALL
log_errors = On
# Not stdout: runJobs.php writes its --result=json payload there, and the
# jobrunner logs severity=ERROR when it cannot parse it.
error_log = /dev/stderr
display_errors = Off

# Upload Config
upload_max_filesize = 64M
post_max_size = 64M